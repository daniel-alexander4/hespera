# Binaries
bin/
*.exe
/isomedia
/hespera

# Release artifacts (cross-compiled binaries + .deb packages)
dist/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*

# Data / runtime
*.sqlite
*.sqlite-wal
*.sqlite-shm
thumbs/

# Project memory
MEMORY.md

# Local scripts
full.docker.reset.sh

# Playwright MCP
.playwright-mcp/

# JS test harness (dev-only; jsdom is never compiled into the Go binary)
node_modules/
package-lock.json

# Billboard chart data is fetched at runtime into DataDir, never committed
internal/billboard/data.json.gz
