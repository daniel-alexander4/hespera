---
phase: 03-fragility-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/helpers.go
  - internal/web/helpers_test.go
  - internal/web/handlers_music.go
  - internal/web/handlers_tv.go
autonomous: true
requirements:
  - FRAG-01

must_haves:
  truths:
    - "URL path ID parsing is handled by pathID() and pathSegment() helpers -- no handler contains its own TrimPrefix+Clean+ParseInt block"
    - "All 6 int64 ID call sites use pathID(r, prefix) instead of inline parsing"
    - "The TV series string-ID call site uses pathSegment(r, prefix) instead of inline parsing"
    - "pathID rejects zero, negative, and non-numeric path segments"
    - "The TV art handler multi-segment pattern is left unchanged"
  artifacts:
    - path: "internal/web/helpers.go"
      provides: "pathID and pathSegment helper functions"
      exports: []
      contains: "func pathID"
    - path: "internal/web/helpers_test.go"
      provides: "Unit tests for pathID and pathSegment"
      contains: "TestPathID"
  key_links:
    - from: "internal/web/handlers_music.go"
      to: "internal/web/helpers.go"
      via: "pathID(r, prefix) calls"
      pattern: "pathID\\(r,"
    - from: "internal/web/handlers_tv.go"
      to: "internal/web/helpers.go"
      via: "pathID and pathSegment calls"
      pattern: "path(ID|Segment)\\(r,"
---

<objective>
Extract duplicated URL path ID parsing into shared helpers and replace all call sites.

Purpose: Eliminate 7 identical TrimPrefix+Clean+ParseInt blocks across music and TV handlers, reducing fragility from copy-paste duplication.
Output: helpers.go with pathID() and pathSegment(), helpers_test.go with unit tests, all handler call sites updated.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fragility-elimination/03-RESEARCH.md

@internal/web/handler.go
@internal/web/handlers_music.go
@internal/web/handlers_tv.go

<interfaces>
<!-- Existing error helpers from Phase 1 that call sites use for error responses -->

From internal/web/errors.go (established in Phase 1):
- httpError(w, code, msg, logMsg, attrs...) -- used for HTTP error responses with server-side logging
- All current pathID call sites use http.NotFound(w, r) on parse failure -- keep this behavior

Current inline pattern (7 identical blocks):
```go
idStr := strings.TrimPrefix(r.URL.Path, "/music/artist/")
idStr = path.Clean("/" + idStr)
idStr = strings.TrimPrefix(idStr, "/")
artistID, err := strconv.ParseInt(idStr, 10, 64)
if err != nil || artistID <= 0 {
    http.NotFound(w, r)
    return
}
```

Call sites to replace:
| File | Lines | Prefix | Variable | Type |
|------|-------|--------|----------|------|
| handlers_music.go | 316-319 | /music/artist/ | artistID | int64 |
| handlers_music.go | 451-453 | /music/album/ | albumID | int64 |
| handlers_music.go | 1042-1044 | /stream/track/ | trackID | int64 |
| handlers_music.go | 1224-1226 | /art/album/ | albumID | int64 |
| handlers_music.go | 1272-1274 | /art/artist/ | artistID | int64 |
| handlers_tv.go | 727-729 | /stream/tv/ | fileID | int64 |
| handlers_tv.go | 172-174 | /tv/series/ | seriesID | string (pathSegment) |

DO NOT touch handlers_tv.go:441 -- this is the TV art handler with a multi-segment pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pathID and pathSegment helpers with tests</name>
  <files>internal/web/helpers.go, internal/web/helpers_test.go</files>
  <behavior>
    - pathID(r, "/music/artist/") with URL "/music/artist/42" returns (42, nil)
    - pathID(r, "/music/artist/") with URL "/music/artist/0" returns (0, error)
    - pathID(r, "/music/artist/") with URL "/music/artist/-5" returns (0, error)
    - pathID(r, "/music/artist/") with URL "/music/artist/abc" returns (0, error)
    - pathID(r, "/music/artist/") with URL "/music/artist/" returns (0, error)
    - pathID(r, "/music/artist/") with URL "/music/artist/../42" returns (42, nil) -- path.Clean normalizes
    - pathSegment(r, "/tv/series/") with URL "/tv/series/12345" returns "12345"
    - pathSegment(r, "/tv/series/") with URL "/tv/series/" returns ""
    - pathSegment(r, "/tv/series/") with URL "/tv/series/../foo" returns "foo" -- path.Clean normalizes
  </behavior>
  <action>
    Create internal/web/helpers.go with two unexported functions:

    1. `pathID(r *http.Request, prefix string) (int64, error)` -- extract positive int64 from URL path after prefix. Apply path.Clean sanitization. Return descriptive error for invalid/zero/negative values.

    2. `pathSegment(r *http.Request, prefix string) string` -- extract sanitized string segment from URL path after prefix. Apply path.Clean sanitization. Return empty string if nothing after prefix.

    Both functions use the same TrimPrefix + path.Clean("/" + seg) + TrimPrefix(seg, "/") pattern currently duplicated across handlers.

    Create internal/web/helpers_test.go with table-driven tests for both functions using httptest.NewRequest. Follow project test pattern: direct conditionals with t.Fatalf(), no assertion libraries.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/web -run "TestPathID|TestPathSegment" -count=1 -v</automated>
  </verify>
  <done>pathID returns correct int64 for valid paths, descriptive errors for invalid/zero/negative. pathSegment returns sanitized string segments. All test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all inline ID parsing with pathID/pathSegment calls</name>
  <files>internal/web/handlers_music.go, internal/web/handlers_tv.go</files>
  <action>
    Replace 7 inline ID-parsing blocks with calls to the new helpers:

    In handlers_music.go, replace 5 blocks:
    1. Lines ~316-319: Replace TrimPrefix+Clean+ParseInt block with `artistID, err := pathID(r, "/music/artist/")` + `if err != nil { http.NotFound(w, r); return }`
    2. Lines ~451-454: Same pattern with `albumID, err := pathID(r, "/music/album/")`
    3. Lines ~1042-1045: Same pattern with `trackID, err := pathID(r, "/stream/track/")`
    4. Lines ~1224-1227: Same pattern with `albumID, err := pathID(r, "/art/album/")`
    5. Lines ~1272-1275: Same pattern with `artistID, err := pathID(r, "/art/artist/")`

    In handlers_tv.go, replace 2 blocks:
    6. Lines ~172-174: Replace TrimPrefix+Clean+TrimPrefix block with `seriesID := pathSegment(r, "/tv/series/")` + keep existing `if seriesID == ""` check
    7. Lines ~727-730: Replace TrimPrefix+Clean+ParseInt block with `fileID, err := pathID(r, "/stream/tv/")`

    DO NOT touch handlers_tv.go:441 (TV art handler) -- it uses a multi-segment pattern that does not match pathID.

    After replacements, remove unused imports if strconv, path, or strings are no longer needed in those files (check carefully -- they likely still use these imports for other purposes).

    Preserve the existing error behavior: all int64 call sites use http.NotFound on failure. The TV series string site keeps its existing empty-string check.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go build ./... && go vet ./... && go test ./... -count=1</automated>
  </verify>
  <done>Zero instances of the 4-line TrimPrefix+Clean+TrimPrefix+ParseInt pattern remain in handlers (except TV art multi-segment). All handlers compile and pass vet. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `go build ./...` -- compiles without errors
2. `go vet ./...` -- no vet warnings
3. `go test ./internal/web -run "TestPathID|TestPathSegment" -count=1` -- helper tests pass
4. `go test ./... -count=1` -- full suite passes
5. Grep confirms no remaining inline pattern: `grep -n "path.Clean.*idStr" internal/web/handlers_*.go` returns empty
</verification>

<success_criteria>
- pathID() and pathSegment() exist in helpers.go with unit tests
- All 6 int64 call sites replaced with pathID(r, prefix)
- TV series string call site replaced with pathSegment(r, prefix)
- TV art handler multi-segment pattern left unchanged
- Full test suite passes, build and vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-fragility-elimination/03-01-SUMMARY.md`
</output>
