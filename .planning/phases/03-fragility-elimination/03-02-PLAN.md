---
phase: 03-fragility-elimination
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/handler.go
  - internal/web/handler_test.go
  - cmd/isomedia/main.go
autonomous: true
requirements:
  - FRAG-02
  - FRAG-03

must_haves:
  truths:
    - "A missing template file on disk causes New() to return an error listing the broken template name"
    - "A broken/unparseable layout template causes New() to return an error immediately -- no fallback layout"
    - "Multiple broken page templates are reported together in a single multi-error"
    - "main.go logs the error and exits with os.Exit(1) when New() fails"
    - "The fallback layout template (line 113) is removed entirely"
    - "Every page in the pages slice is validated to exist in the compiled tpls map after compilation"
  artifacts:
    - path: "internal/web/handler.go"
      provides: "New() returning (*Handler, error) with template validation"
      contains: "func New(d Deps) (*Handler, error)"
    - path: "internal/web/handler_test.go"
      provides: "Tests for template validation and fail-fast behavior"
      contains: "TestNewMissingTemplate"
    - path: "cmd/isomedia/main.go"
      provides: "Updated caller handling New() error"
      contains: "h, err := web.New"
  key_links:
    - from: "cmd/isomedia/main.go"
      to: "internal/web/handler.go"
      via: "New() error return"
      pattern: "h, err := web\\.New"
    - from: "internal/web/handler.go"
      to: "errors.Join"
      via: "multi-error aggregation for page parse failures"
      pattern: "errors\\.Join"
---

<objective>
Make template compilation fail fast at startup with descriptive errors instead of silently degrading at runtime.

Purpose: Eliminate silent template failures that produce runtime 500s. Layout failure, page parse failure, or missing templates now prevent the server from starting and report exactly what is wrong.
Output: handler.go with New() returning error, handler_test.go with validation tests, main.go updated to handle the error.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fragility-elimination/03-RESEARCH.md

@internal/web/handler.go
@cmd/isomedia/main.go

<interfaces>
<!-- Current New() signature and template compilation flow -->

Current signature (handler.go:36):
```go
func New(d Deps) *Handler
```

Target signature:
```go
func New(d Deps) (*Handler, error)
```

Current error handling in New():
- Layout parse failure (line 111-114): logs error, uses fallback layout -- REMOVE fallback, return error
- Page clone failure (line 119-122): logs error, continue -- collect into multi-error
- Page parse failure (line 127-129): logs error, continue -- collect into multi-error

Current caller in main.go (line 38-41):
```go
h := web.New(web.Deps{
    Cfg: cfg,
    DB:  dbConn,
})
```

Pages list (handler.go:42-65): 22 page templates, serves as single source of truth.
Validation: after compilation loop, check every page in pages exists in tpls map.

Template directory: web/templates/
Layout: web/templates/layout.html
Partials: web/templates/partials_*.html
Pages: web/templates/{page}.html
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change New() to return error and validate templates at startup</name>
  <files>internal/web/handler.go, internal/web/handler_test.go</files>
  <behavior>
    - New() with all valid templates in temp dir returns (*Handler, nil) with all templates compiled
    - New() with missing layout.html returns (nil, error) containing "layout template"
    - New() with unparseable layout.html returns (nil, error) containing "layout template"
    - New() with one missing page template returns (nil, error) containing the page name
    - New() with multiple missing page templates returns (nil, error) listing ALL broken pages (not just first)
    - New() with all valid templates populates tpls map with all page entries
  </behavior>
  <action>
    Modify internal/web/handler.go New() function:

    1. Change signature from `func New(d Deps) *Handler` to `func New(d Deps) (*Handler, error)`

    2. Layout parse failure (line 111-114): Replace fallback with `return nil, fmt.Errorf("layout template: %w", err)`. Delete the fallback template entirely (line 113).

    3. Page compilation loop (lines 116-132): Collect errors instead of logging-and-continuing.
       - Clone failure: append `fmt.Errorf("template %s: clone failed: %w", p, cloneErr)` to errs slice
       - Parse failure: append `fmt.Errorf("template %s: %w", p, err)` to errs slice
       - After loop: if `len(errs) > 0`, return `nil, fmt.Errorf("template compilation failed:\n%w", errors.Join(errs...))`

    4. Post-loop validation: After the compilation loop succeeds, verify every page in the pages slice has an entry in the tpls map. This catches cases where a page was skipped silently. If any missing, return error.

    5. Add `"errors"` to imports. Add `"fmt"` if not already imported.

    6. Return `h, nil` at end.

    Create internal/web/handler_test.go with tests:
    - Tests need a temp directory with template files. Use t.TempDir() to create web/templates/ structure.
    - Create minimal valid layout.html and page templates for the happy path.
    - Test broken layout: write invalid Go template syntax to layout.html.
    - Test missing page: remove one page file from the temp dir pages list.
    - Test multiple broken pages: remove several page files.

    IMPORTANT: The test needs to handle the fact that New() reads templates relative to the working directory. Either:
    (a) Change working directory to the temp dir in the test (using os.Chdir, with restore), OR
    (b) Refactor New() to accept a template directory parameter -- but this is a larger change.

    Recommended approach: Use os.Chdir in tests since this is the simplest approach and follows the existing convention where templates are loaded relative to CWD. Save and restore the original directory using t.Cleanup.

    The test also needs to provide valid Deps (config + DB). For template validation tests, a nil DB is fine since New() only uses DB for jobs.New() and auth.New() which are called after template compilation. If nil DB panics, create a minimal in-memory SQLite using the existing openTestDB pattern from other test files, or simply skip the test if it panics. The simplest approach: check if New() touches DB before template validation -- if yes, the test needs a real DB. If New() does template work first, a nil DB will work because execution returns on template error before reaching DB usage.

    Actually, examining New() carefully: jobs.New(d.DB) and auth.New(d.Cfg, d.DB) are called at lines 139-141, AFTER template compilation. So for error-path tests (missing/broken templates), New() will return error before reaching those lines. For the happy-path test, a real DB is needed. Use the openTestDB pattern (temp SQLite) or provide a minimal DB.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/web -run "TestNew" -count=1 -v</automated>
  </verify>
  <done>New() returns (*Handler, error). Layout failure returns immediate error. Page failures are collected into multi-error listing all broken pages. Fallback layout template is removed. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update main.go to handle New() error return</name>
  <files>cmd/isomedia/main.go</files>
  <action>
    Update the single call site in cmd/isomedia/main.go (line 38):

    BEFORE:
    ```go
    h := web.New(web.Deps{
        Cfg: cfg,
        DB:  dbConn,
    })
    ```

    AFTER:
    ```go
    h, err := web.New(web.Deps{
        Cfg: cfg,
        DB:  dbConn,
    })
    if err != nil {
        slog.Error("web handler initialization failed", "err", err)
        os.Exit(1)
    }
    ```

    This follows the exact same error handling pattern already used for cfg.Validate(), db.Open(), and db.Migrate() in main.go -- log with slog.Error and os.Exit(1).

    No new imports needed -- slog, os already imported.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go build ./cmd/isomedia && go vet ./... && go test ./... -count=1</automated>
  </verify>
  <done>main.go handles New() error with slog.Error + os.Exit(1). Build succeeds. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `go build ./...` -- compiles without errors (signature change propagated to all callers)
2. `go vet ./...` -- no vet warnings
3. `go test ./internal/web -run "TestNew" -count=1` -- template validation tests pass
4. `go test ./... -count=1` -- full suite passes
5. Confirm fallback layout removed: `grep -n "fallback\|Must(template" internal/web/handler.go` returns empty
6. Confirm New() returns error: `grep -n "func New" internal/web/handler.go` shows `(*Handler, error)`
</verification>

<success_criteria>
- New() returns (*Handler, error) -- no more silent template degradation
- Fallback layout template completely removed
- Layout parse failure returns error immediately
- Page parse failures collected and reported as multi-error listing all broken templates
- main.go handles the error with slog.Error + os.Exit(1)
- Full test suite passes, build and vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-fragility-elimination/03-02-SUMMARY.md`
</output>
