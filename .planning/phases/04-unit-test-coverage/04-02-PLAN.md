---
phase: 04-unit-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tvscan/scanner_test.go
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "upsertTVFile inserts a tv_series_files row and a tv_series_identities row"
    - "upsertTVFile with nil identity creates a needs_fix identity with empty fields"
    - "upsertTVFile on conflict updates file metadata (size, mtime) without duplicating"
    - "upsertTVFile preserves identity for resolved/skipped files on rescan (BUG-01 fix)"
    - "upsertTVFile overwrites identity for needs_fix files on rescan"
    - "pruneMissingFiles removes DB rows for files that no longer exist on disk"
    - "pruneMissingFiles preserves DB rows for files that still exist"
  artifacts:
    - path: "internal/tvscan/scanner_test.go"
      provides: "TV scanner upsert, rescan, and prune tests"
      min_lines: 120
  key_links:
    - from: "internal/tvscan/scanner_test.go"
      to: "internal/db"
      via: "openTestDB(t) using isodb.Open + isodb.Migrate"
      pattern: "isodb\\.Open|isodb\\.Migrate"
    - from: "internal/tvscan/scanner_test.go"
      to: "internal/tvscan/scanner.go"
      via: "calls upsertTVFile and pruneMissingFiles"
      pattern: "upsertTVFile|pruneMissingFiles"
---

<objective>
Add unit tests for the TV scanner package covering upsertTVFile (insert, update, rescan behavior including BUG-01 fix) and pruneMissingFiles (TEST-03).

Purpose: Lock down the TV scanner's DB interaction logic and verify that the Phase 2 BUG-01 fix (resolved files preserved on rescan) is regression-tested.
Output: Test file in internal/tvscan/ with passing tests.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unit-test-coverage/04-RESEARCH.md

@internal/tvscan/scanner.go
@internal/tvscan/identify.go
@internal/db/db.go
@internal/web/handler_test.go (for openTestDB pattern reference)

<interfaces>
<!-- Key types and functions the executor needs from the tvscan package -->

From internal/tvscan/scanner.go:
```go
type Scanner struct {
    Cfg config.Config
    DB  *sql.DB
}

func New(cfg config.Config, db *sql.DB) *Scanner

// unexported -- test file in same package can access directly:
func (s *Scanner) upsertTVFile(ctx context.Context, libraryID int64, resolvedPath, container string, fileSize, mtimeUnix int64, streamInfoJSON string, ident *EpisodeIdentity) error
func (s *Scanner) pruneMissingFiles(ctx context.Context, libraryID int64, root string) error
```

From internal/tvscan/identify.go:
```go
type EpisodeIdentity struct {
    ShowTitle      string
    SeasonNumber   int
    EpisodeNumbers []int
    Confidence     float64
    Method         string // "sxe", "x_format", "season_dir"
}
```

DB tables used:
- tv_series_files: id, library_id, abs_path, container, file_size_bytes, mtime_unix, stream_info_json
- tv_series_identities: file_id, status, guessed_title, season_number, episode_numbers_csv, match_confidence, match_method, provider, series_id
  - ON CONFLICT(file_id) DO UPDATE ... WHERE status NOT IN ('resolved', 'skipped') -- this is the BUG-01 fix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TV scanner upsertTVFile and pruneMissingFiles tests</name>
  <files>internal/tvscan/scanner_test.go</files>
  <behavior>
    - TestUpsertTVFile_Insert: new file creates tv_series_files + tv_series_identities rows with correct values
    - TestUpsertTVFile_NilIdentity: nil ident creates identity with status=needs_fix, empty guessed_title, season_number=-1
    - TestUpsertTVFile_UpdateOnConflict: re-upserting same path updates file metadata (size, mtime) without creating duplicate rows
    - TestUpsertTVFile_RescanPreservesResolved: file with status=resolved keeps its identity (guessed_title, series_id, provider) when upsertTVFile is called again with different identity data (BUG-01 fix)
    - TestUpsertTVFile_RescanPreservesSkipped: file with status=skipped also keeps identity on rescan
    - TestUpsertTVFile_RescanOverwritesNeedsFix: file with status=needs_fix gets identity overwritten on rescan
    - TestPruneMissingFiles_RemovesMissing: file in DB but not on disk gets deleted from tv_series_files
    - TestPruneMissingFiles_PreservesExisting: file in DB that still exists on disk is preserved
  </behavior>
  <action>
Create `internal/tvscan/scanner_test.go` in the `tvscan` package (same package, so unexported methods are accessible).

1. Add an `openTestDB(t)` helper identical to the pattern in `internal/web/handler_test.go`: creates temp SQLite via `isodb.Open` + `isodb.Migrate`, registers cleanup.

2. Add a `seedLibrary(t, db, name, libType, rootPath)` helper that inserts a library row and returns its ID.

3. Add `TestUpsertTVFile` with subtests:
   - "insert new file": create Scanner{DB: db}, call upsertTVFile with libraryID=1, a path, container="mkv", size=1024, mtime=1700000000, streamJSON="{}", and a valid EpisodeIdentity{ShowTitle: "Breaking Bad", SeasonNumber: 1, EpisodeNumbers: []int{1}, Confidence: 0.72, Method: "sxe"}. Query tv_series_files to verify row exists with correct values. Query tv_series_identities to verify guessed_title="Breaking Bad", season_number=1, episode_numbers_csv="1", status="needs_fix".
   - "nil identity": call upsertTVFile with ident=nil. Verify identity row has guessed_title="", season_number=-1, episode_numbers_csv="", status="needs_fix".
   - "update on conflict": call upsertTVFile twice for same library+path but different fileSize (1024 then 2048). Verify only 1 row in tv_series_files. Verify file_size_bytes updated to 2048.
   - "rescan preserves resolved": insert file, then manually UPDATE tv_series_identities SET status='resolved', provider='tmdb', series_id='12345'. Call upsertTVFile again with a different EpisodeIdentity{ShowTitle: "Different Show", SeasonNumber: 2, ...}. Verify guessed_title is still "Breaking Bad" (original), series_id still "12345", status still "resolved". The WHERE clause prevents overwrite.
   - "rescan preserves skipped": same pattern as resolved but with status='skipped'. Verify identity preserved.
   - "rescan overwrites needs_fix": insert file with status needs_fix (default). Call upsertTVFile with new identity. Verify guessed_title updated to the new value.

4. Add `TestPruneMissingFiles` with subtests:
   - "removes missing files": create a tmpDir as root. Insert a tv_series_files row with abs_path pointing to a file under root that does NOT exist on disk. Call pruneMissingFiles. Verify row is deleted.
   - "preserves existing files": create a tmpDir, write a real file in it. Insert tv_series_files row with that path. Call pruneMissingFiles. Verify row still exists.
   - "ignores files outside root": insert a row with abs_path outside root. Call pruneMissingFiles. Verify row is NOT deleted (pruneMissingFiles skips paths outside root).

Use `Scanner{DB: db}` directly -- no need for Config for these functions.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/tvscan/ -run "TestUpsertTVFile|TestPruneMissingFiles" -count=1 -v</automated>
  </verify>
  <done>upsertTVFile has passing tests for insert, nil identity, conflict update, and BUG-01 rescan protection (resolved, skipped, needs_fix). pruneMissingFiles has passing tests for removing missing files and preserving existing ones. All tests run via `go test ./internal/tvscan/` without failures.</done>
</task>

</tasks>

<verification>
All TV scanner tests pass:
```bash
go test ./internal/tvscan/ -count=1 -v
```

Existing identify_test.go still passes:
```bash
go test ./internal/tvscan/ -run TestIdentifyFile -count=1
```

Full test suite still passes (no regressions):
```bash
go test ./... -count=1
```
</verification>

<success_criteria>
- `internal/tvscan/scanner_test.go` exists with tests for upsertTVFile and pruneMissingFiles
- BUG-01 rescan behavior is explicitly tested (resolved files preserved)
- All tests pass via `go test ./internal/tvscan/ -count=1`
- No regressions in other packages via `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/04-unit-test-coverage/04-02-SUMMARY.md`
</output>
