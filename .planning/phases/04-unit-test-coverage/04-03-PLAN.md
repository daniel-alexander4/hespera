---
phase: 04-unit-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/handlers_music_test.go
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "GET /music returns 200 with valid HTML"
    - "POST /music returns 405 Method Not Allowed"
    - "GET /music/artist/42 returns 200 when artist exists in DB"
    - "GET /music/artist/999 returns 404 when artist does not exist"
    - "GET /music/album/42 returns 200 when album exists with tracks"
    - "GET /music/album/999 returns 404 when album does not exist"
    - "POST /music/artist/42 returns 405 Method Not Allowed"
  artifacts:
    - path: "internal/web/handlers_music_test.go"
      provides: "Music handler endpoint tests"
      min_lines: 80
  key_links:
    - from: "internal/web/handlers_music_test.go"
      to: "internal/web/handler_test.go"
      via: "reuses openTestDB, setupTemplateDir, withChdir helpers"
      pattern: "openTestDB|setupTemplateDir|withChdir"
    - from: "internal/web/handlers_music_test.go"
      to: "internal/web/router.go"
      via: "tests routes through Handler.Router()"
      pattern: "Router\\(\\)"
---

<objective>
Add unit tests for music handler endpoints verifying routing, ID parsing via shared pathID helper, and generic error responses (TEST-04).

Purpose: Verify that music browse handlers return correct HTTP status codes and use the shared pathID helper correctly, catching regressions in routing and error sanitization from Phases 1 and 3.
Output: Test file in internal/web/ with passing tests.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unit-test-coverage/04-RESEARCH.md

@internal/web/handlers_music.go
@internal/web/router.go
@internal/web/handler_test.go (existing test infrastructure)
@internal/web/helpers_test.go (pathID/pathSegment tests)

<interfaces>
<!-- Existing test helpers in handler_test.go that this plan reuses -->

From internal/web/handler_test.go:
```go
func openTestDB(t *testing.T) *sql.DB       // temp SQLite with migrations
func setupTemplateDir(t *testing.T, dir string) // minimal template dir with all 22 pages
func withChdir(t *testing.T, dir string)     // chdir for template loading
```

From internal/web/handler.go:
```go
type Deps struct {
    Cfg config.Config
    DB  *sql.DB
}

func New(d Deps) (*Handler, error) // compiles templates, starts jobs, inits auth
func (h *Handler) Router() http.Handler // returns mux with all routes
```

Key routes being tested:
- GET /music -> musicHome (200)
- GET /music/artist/{id} -> musicArtistAlbums (200 or 404)
- GET /music/album/{id} -> musicAlbumTracks (200 or 404)
- POST on any GET-only route -> 405

Auth middleware bypass: Config with default AuthEnabled (false when AuthSessionSecret is empty) means auth middleware is nil, no login redirects.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Music handler endpoint tests</name>
  <files>internal/web/handlers_music_test.go</files>
  <behavior>
    - TestMusicHome_200: GET /music returns 200
    - TestMusicHome_405: POST /music returns 405
    - TestMusicArtistAlbums_200: GET /music/artist/{id} returns 200 when artist exists
    - TestMusicArtistAlbums_404: GET /music/artist/999 returns 404 when artist does not exist
    - TestMusicArtistAlbums_InvalidID: GET /music/artist/abc returns 404 (pathID rejects non-numeric)
    - TestMusicArtistAlbums_405: POST /music/artist/1 returns 405
    - TestMusicAlbumTracks_200: GET /music/album/{id} returns 200 when album+tracks exist
    - TestMusicAlbumTracks_404: GET /music/album/999 returns 404 when album does not exist
  </behavior>
  <action>
Create `internal/web/handlers_music_test.go` in the `web` package.

1. Add a `newTestHandler(t)` helper function that:
   - Creates t.TempDir() for both DataDir and MediaRoot
   - Calls setupTemplateDir(t, dir) to create template files
   - Calls withChdir(t, dir) to set working directory
   - Calls openTestDB(t) for a real migrated SQLite
   - Creates Handler via New(Deps{Cfg: config.Config{DataDir: dir, MediaRoot: dir}, DB: db})
   - Returns (*Handler, *sql.DB)
   - Fatals on any error

2. Add a `seedMusicData(t, db)` helper that inserts a complete music data set in FK-safe order:
   - Library row (type=music, root_path=some path)
   - Artist row (library_id, name="Test Artist") -- include bio and bio_source_url as empty strings to satisfy the SELECT in musicArtistAlbums
   - Album row (library_id, artist_id, album_artist_id=artist_id, title="Test Album", year=2024, is_compilation=0)
   - Track row (library_id, artist_id, album_id, title="Track 1", track_no=1, disc_no=1, abs_path="/test/track1.mp3", mime_type="audio/mpeg")
   - Returns libraryID, artistID, albumID, trackID

3. Write table-driven test `TestMusicHandlers` with subtests using `t.Run`:

   For each test case: method, path, expected status code, whether to seed data.

   Test cases:
   - "GET /music 200": GET /music -> 200 (musicHome renders even without data)
   - "POST /music 405": POST /music -> 405
   - "GET /music/artist/{id} 200": seed data, GET /music/artist/{artistID} -> 200
   - "GET /music/artist/999 404": GET /music/artist/999 -> 404 (no such artist)
   - "GET /music/artist/abc 404": GET /music/artist/abc -> 404 (pathID rejects)
   - "POST /music/artist/{id} 405": POST /music/artist/1 -> 405
   - "GET /music/album/{id} 200": seed data, GET /music/album/{albumID} -> 200
   - "GET /music/album/999 404": GET /music/album/999 -> 404 (no such album)

   Implementation approach: since handler construction (New) is expensive (template compilation, os.Chdir), create ONE handler + router per test function. Seed data once. Run subtests that need data after seeding.

   Actually, better to group: create handler once at top, seed once, then run all cases. Use a struct-based test table. For cases that need a dynamic ID (artistID, albumID), format the URL after seeding.

   Pattern:
   ```go
   func TestMusicHandlers(t *testing.T) {
       h, db := newTestHandler(t)
       router := h.Router()
       libID, artistID, albumID, _ := seedMusicData(t, db)
       // ... run subtests
   }
   ```

   For each test case, create httptest.NewRequest with method+path, get httptest.NewRecorder, call router.ServeHTTP, assert response code.

   Do NOT use t.Parallel() (os.Chdir is process-global).

4. Verify error responses don't leak internal details: for 404 responses, check body does NOT contain SQL error text or file paths. For 500 responses (if any triggered), check body is generic.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/web/ -run "TestMusicHandlers" -count=1 -v</automated>
  </verify>
  <done>Music handler tests verify correct HTTP status codes for GET/POST on /music, /music/artist/{id}, /music/album/{id}. Tests cover 200 success, 404 not found, 405 method not allowed, and invalid ID rejection. All pass via `go test ./internal/web/`.</done>
</task>

</tasks>

<verification>
Music handler tests pass:
```bash
go test ./internal/web/ -run TestMusicHandlers -count=1 -v
```

Existing web tests still pass:
```bash
go test ./internal/web/ -count=1 -v
```

Full test suite still passes:
```bash
go test ./... -count=1
```
</verification>

<success_criteria>
- `internal/web/handlers_music_test.go` exists with tests for musicHome, musicArtistAlbums, musicAlbumTracks
- Tests verify 200, 404, 405 status codes
- Tests use shared pathID helper (via Router routing) -- not testing pathID directly (that's in helpers_test.go)
- All tests pass via `go test ./internal/web/ -count=1`
- No regressions in other packages via `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/04-unit-test-coverage/04-03-SUMMARY.md`
</output>
