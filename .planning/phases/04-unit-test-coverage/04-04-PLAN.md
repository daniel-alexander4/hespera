---
phase: 04-unit-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/handlers_tv_test.go
  - internal/web/handlers_settings_test.go
autonomous: true
requirements:
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "GET /tv returns 200 with valid HTML"
    - "POST /tv returns 405 Method Not Allowed"
    - "GET /tv/series/{id} returns 404 when series metadata does not exist in cache"
    - "GET /tv/series/{id} returns 200 when metadata cache has a valid show entry"
    - "GET /libraries returns 200 listing seeded libraries"
    - "POST /libraries/new creates a library row in DB and redirects"
    - "POST /libraries/new with empty name returns 400"
    - "POST /libraries/scan with valid music library ID returns redirect (job enqueued)"
    - "POST /libraries/scan with nonexistent library ID returns 404"
    - "POST /libraries/delete with valid ID deletes library and redirects"
  artifacts:
    - path: "internal/web/handlers_tv_test.go"
      provides: "TV handler endpoint tests"
      min_lines: 60
    - path: "internal/web/handlers_settings_test.go"
      provides: "Settings/library handler endpoint tests"
      min_lines: 80
  key_links:
    - from: "internal/web/handlers_tv_test.go"
      to: "internal/web/handler_test.go"
      via: "reuses openTestDB, setupTemplateDir, withChdir helpers"
      pattern: "openTestDB|setupTemplateDir|withChdir"
    - from: "internal/web/handlers_settings_test.go"
      to: "internal/web/handler_test.go"
      via: "reuses openTestDB, setupTemplateDir, withChdir helpers"
      pattern: "openTestDB|setupTemplateDir|withChdir"
    - from: "internal/web/handlers_settings_test.go"
      to: "internal/web/router.go"
      via: "tests library CRUD routes through Handler.Router()"
      pattern: "Router\\(\\)"
---

<objective>
Add unit tests for TV handler endpoints (TEST-05) and settings/library handler endpoints (TEST-06), verifying routing, ID parsing, error responses, and library CRUD operations.

Purpose: Complete the handler test coverage for TV browse pages and settings/library management, verifying correct HTTP behavior and DB state changes for CRUD operations.
Output: Two test files in internal/web/ with passing tests.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unit-test-coverage/04-RESEARCH.md

@internal/web/handlers_tv.go
@internal/web/handlers_settings.go
@internal/web/router.go
@internal/web/handler_test.go (existing test infrastructure)

<interfaces>
<!-- Existing test helpers in handler_test.go -->

From internal/web/handler_test.go:
```go
func openTestDB(t *testing.T) *sql.DB
func setupTemplateDir(t *testing.T, dir string)
func withChdir(t *testing.T, dir string)
```

From internal/web/handler.go:
```go
type Deps struct { Cfg config.Config; DB *sql.DB }
func New(d Deps) (*Handler, error)
func (h *Handler) Router() http.Handler
```

Key TV routes:
- GET /tv -> tvSeriesList (200; exact path match via r.URL.Path != "/tv" guard)
- GET /tv/series/{seriesID} -> tvSeriesDetail (200 or 404; uses pathSegment)
- GET /tv/season/?series=X&season=N -> tvSeasonDetail (200 or 404)

Key Settings routes:
- GET /libraries -> libraries (200)
- GET /libraries/new -> librariesNew GET (200)
- POST /libraries/new -> librariesNew POST (creates library, redirects 303)
- POST /libraries/scan -> librariesScan (enqueues job, redirects 303)
- POST /libraries/delete -> librariesDelete (deletes library, redirects 303)
- GET /settings -> settings (200)

TV handler note: tvSeriesDetail reads from tv_series_metadata_cache. To test 200, seed a cache row with entity_key="show:{seriesID}" and valid JSON payload.

Settings handler note: librariesNew POST validates root_path starts with Config.MediaRoot. librariesScan enqueues a job via h.jobs -- the Handler.jobs is initialized in New().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TV handler endpoint tests</name>
  <files>internal/web/handlers_tv_test.go</files>
  <behavior>
    - TestTVSeriesList_200: GET /tv returns 200
    - TestTVSeriesList_405: POST /tv returns 405
    - TestTVSeriesDetail_404: GET /tv/series/99999 returns 404 when no metadata cache exists
    - TestTVSeriesDetail_200: GET /tv/series/{id} returns 200 when metadata cache has valid show JSON
    - TestTVSeriesDetail_405: POST /tv/series/123 returns 405
  </behavior>
  <action>
Create `internal/web/handlers_tv_test.go` in the `web` package.

1. Reuse the `newTestHandler(t)` helper from handlers_music_test.go (same package, no import needed). If Plan 03 hasn't been executed yet, define it here -- but since both are Wave 1 and in the same package, the executor should check if it exists first and only define it if missing. Use a build guard: define newTestHandler only if not already defined. Actually, since Go test compilation includes all _test.go files in the package, just use it without re-defining. If Plan 03 runs first, it's already defined. If Plan 04 runs first, define it here and Plan 03 can use it. To handle either execution order: define newTestHandler in this file with a different name like `newTVTestHandler` -- OR define it identically (Go will error on duplicate).

Best approach: define a `testHandler(t)` function here that does the same setup. If Plan 03's newTestHandler already exists, this will cause a compile error. To avoid this, use a slightly different name: `setupTestHandler(t)`. Actually, the simplest approach is to define a `func newTestHandler(t *testing.T) (*Handler, *sql.DB)` only if it doesn't exist. Since Go compiles all test files together, we can't have two definitions.

Resolution: Define newTestHandler ONCE. Since both plans are Wave 1 and may execute in any order, put the definition here with a note for the executor: "If newTestHandler is already defined in handlers_music_test.go, do not redefine it." If this plan executes first, define it. If Plan 03 executed first, just use it.

2. Add a `seedTVMetadata(t, db, seriesID)` helper that inserts a tv_series_metadata_cache row:
   - entity_key = "show:" + seriesID
   - lang = "en"
   - payload_json = valid JSON: `{"name":"Test Show","first_air_date":"2024-01-15","status":"Returning Series","overview":"A test show","poster_path":"/test.jpg","seasons":[],"genres":[]}`
   This is what tvSeriesDetail queries.

3. Write `TestTVHandlers` with subtests:
   - "GET /tv 200": GET /tv -> 200 (tvSeriesList renders empty list)
   - "POST /tv 405": POST /tv -> 405
   - "GET /tv/series/99999 404": GET /tv/series/99999 -> 404 (no cache row)
   - "GET /tv/series/{id} 200": seed metadata for seriesID="12345", GET /tv/series/12345 -> 200
   - "POST /tv/series/123 405": POST /tv/series/123 -> 405

   Note: tvSeriesList does an exact path check (`r.URL.Path != "/tv"`), so GET /tv should match. GET /tv/ with trailing slash would 404 -- but we're testing the happy path.

4. For 404 responses, verify body does not contain SQL error text.
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/web/ -run "TestTVHandlers" -count=1 -v</automated>
  </verify>
  <done>TV handler tests verify correct HTTP status codes for GET/POST on /tv and /tv/series/{id}. Tests cover 200 success with seeded metadata, 404 not found, and 405 method not allowed. All pass via `go test ./internal/web/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings and library handler endpoint tests</name>
  <files>internal/web/handlers_settings_test.go</files>
  <behavior>
    - TestLibraries_200: GET /libraries returns 200
    - TestLibraries_405: POST /libraries returns 405
    - TestLibrariesNew_GET_200: GET /libraries/new returns 200
    - TestLibrariesNew_POST_Creates: POST /libraries/new with valid name/type/root_path creates library, returns 303 redirect to /libraries
    - TestLibrariesNew_POST_EmptyName_400: POST /libraries/new with empty name returns 400
    - TestLibrariesNew_POST_InvalidType_400: POST /libraries/new with type="invalid" returns 400
    - TestLibrariesNew_POST_BadRoot_400: POST /libraries/new with root_path outside MediaRoot returns 400
    - TestLibrariesScan_POST: POST /libraries/scan with valid library ID enqueues job, returns 303 redirect
    - TestLibrariesScan_NotFound: POST /libraries/scan with nonexistent library ID returns 404
    - TestLibrariesDelete_POST: POST /libraries/delete with valid ID deletes library, returns 303 redirect
    - TestSettings_200: GET /settings returns 200
  </behavior>
  <action>
Create `internal/web/handlers_settings_test.go` in the `web` package.

1. Reuse `newTestHandler(t)` from the same package (defined in handlers_music_test.go or handlers_tv_test.go).

2. Write `TestSettingsHandlers` with subtests:
   - "GET /settings 200": GET /settings -> 200
   - "POST /settings 405": POST /settings -> 405

3. Write `TestLibraryHandlers` with subtests:

   a. "GET /libraries 200": GET /libraries -> 200

   b. "POST /libraries 405": POST /libraries -> 405

   c. "GET /libraries/new 200": GET /libraries/new -> 200

   d. "POST /libraries/new creates library": POST /libraries/new with form data (name="Test Music", type="music", root_path={mediaRoot}/music). The root_path MUST start with Config.MediaRoot. Create the directory first via os.MkdirAll. Expect 303 redirect to /libraries. Verify library row exists in DB.

   e. "POST /libraries/new empty name 400": POST with name="" -> 400

   f. "POST /libraries/new invalid type 400": POST with type="invalid" -> 400

   g. "POST /libraries/new bad root 400": POST with root_path="/outside/media" -> 400

   h. "POST /libraries/scan enqueues job": First insert a music library via SQL. POST /libraries/scan with form data id={libraryID}. Expect 303 redirect. Verify a scan_jobs row was created in DB.

   i. "POST /libraries/scan not found": POST /libraries/scan with id=99999 -> 404

   j. "POST /libraries/delete": Insert a library, POST /libraries/delete with form data id={libraryID}. Expect 303 redirect. Verify library row is gone from DB.

   For POST requests with form data, set Content-Type to "application/x-www-form-urlencoded" and use `strings.NewReader("name=Test&type=music&root_path=...")` as body.

   Note: librariesScan triggers job enqueue. The Handler's jobs service is initialized in New() -- it creates a real jobs.Service with a background worker. The Enqueue call will succeed and create a scan_jobs row. The actual scan executor will fail (no real files), but the HTTP response happens before execution. The test only verifies the HTTP response (303) and job creation.

   After each test that modifies DB state, the test DB is fresh (each test function gets its own via newTestHandler), so no cleanup needed between subtests IF we create a fresh handler per test function. Since os.Chdir is global, we must NOT run these in parallel. We can create one handler per test function and run subtests sequentially.

   Actually, since newTestHandler calls withChdir which is not parallel-safe, and we have multiple test functions in this file that each call newTestHandler, we need to run them sequentially. Don't use t.Parallel().
  </action>
  <verify>
    <automated>cd /home/dan/repos/isomedia && go test ./internal/web/ -run "TestSettingsHandlers|TestLibraryHandlers" -count=1 -v</automated>
  </verify>
  <done>Settings and library handler tests verify correct HTTP status codes and DB state changes. Library CRUD (create via POST, list via GET, delete via POST, scan trigger via POST) is tested. Validation errors (empty name, invalid type, bad root path) return 400. All pass via `go test ./internal/web/`.</done>
</task>

</tasks>

<verification>
TV and settings handler tests pass:
```bash
go test ./internal/web/ -run "TestTVHandlers|TestSettingsHandlers|TestLibraryHandlers" -count=1 -v
```

All web tests pass together:
```bash
go test ./internal/web/ -count=1 -v
```

Full test suite still passes:
```bash
go test ./... -count=1
```
</verification>

<success_criteria>
- `internal/web/handlers_tv_test.go` exists with tests for tvSeriesList, tvSeriesDetail
- `internal/web/handlers_settings_test.go` exists with tests for libraries, librariesNew, librariesScan, librariesDelete, settings
- Library CRUD tests verify DB state changes (row created, row deleted)
- All tests pass via `go test ./internal/web/ -count=1`
- No regressions in other packages via `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/04-unit-test-coverage/04-04-SUMMARY.md`
</output>
